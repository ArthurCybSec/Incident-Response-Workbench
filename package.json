{
  "name": "incident-response-workbench",
  "version": "1.0.0",
  "private": true,
  "description": "Local-first Windows and Linux log triage workbench",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
